#!/bin/bash

# Install MongoDB tools if they aren't available
# You might need to adapt these commands according to the specific environment
# sudo apt-get install mongo-tools

# Download the catalog.json file
curl -O https://cf-courses-data.s3.us.cloud-object-storage.appdomain.cloud/IBM-DB0321EN-SkillsNetwork/nosql/catalog.json
